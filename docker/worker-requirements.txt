# ══════════════════════════════════════════════════════════════════════
#  AWReason Queue Worker – Python dependencies
# ══════════════════════════════════════════════════════════════════════

# ── Config ───────────────────────────────────────────────────────────
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0

# ── Azure Identity & Blob Storage (MI-based, no SAS) ────────────────
azure-identity>=1.17,<2.0
azure-storage-blob>=12.22,<13.0

# ── Azure Service Bus ────────────────────────────────────────────────
azure-servicebus>=7.12,<8.0

# ── Azure OpenAI ─────────────────────────────────────────────────────
openai>=1.40,<2.0

# ── HTTP client (platform callback) ──────────────────────────────────
httpx>=0.27
aiohttp>=3.9,<4.0

# ── Telemetry (optional – import-guarded) ────────────────────────────
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-grpc>=1.25

# ── awreason engine transitive deps ──────────────────────────────────
PyMuPDF>=1.24
Pillow>=10.2
python-docx>=1.1
markdownify>=0.13
